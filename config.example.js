// config.example.js — SAFE TO COMMIT. This is a template only.
//
// Setup instructions:
// 1. Copy this file and rename the copy to "config.js"
// 2. Get a free API key at https://aistudio.google.com/apikey
// 3. Paste your key below in place of "YOUR_API_KEY_HERE"
// 4. config.js is gitignored, so your real key never gets pushed to GitHub
//
// If you're deploying to GitHub Pages and need Gemma to work on the live
// site (not just localhost), see the README section on deployment options,
// since GitHub Pages can't hide a client-side key from public view either.

window.APP_CONFIG = {
  GOOGLE_API_KEY: "YOUR_API_KEY_HERE"
};
